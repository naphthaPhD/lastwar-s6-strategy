# 2026-06-17 JDX power screenshot OCR

## 1. Executive summary

JDX戦力スクショの2026-06-17アップ分を確認し、静止画 `IMG_2340.PNG` - `IMG_2377.PNG` の38枚をApple Vision OCRで読み取った。

今回は `現在週` タブがヘッダーのみで、戦力分析も0人計算になっていた。そのため、即時差分反映ではなく、まず `20260531_戦力アンケート` などの基準名簿から `現在週` を復元し、その後OCR候補を反映する必要がある。

## 2. Context

- 対象フォルダ: `/Users/mba2025/Library/CloudStorage/Dropbox-FIT/訓北﨑/lastwar/S6/JDX戦力スクショ/inbox`
- 対象画像: `IMG_2340.PNG` - `IMG_2377.PNG`
- 投票者ポップアップ: `IMG_2340.PNG` - `IMG_2363.PNG`
- コメント欄スクショ: `IMG_2364.PNG` - `IMG_2377.PNG`
- 対象シート: Google Sheets `JDX戦力など`
- 対象タブ: `現在週`, `戦力分析`

## 3. Key facts

- 静止画OCR行数: 998行
- 投票OCR候補: 81件
- 投票OCRの名寄せ可能候補: 79件
- コメントOCR候補: 72件
- コメントOCRの名寄せ可能候補: 70件
- 更新候補行: 80名
- `safe_to_apply=True` 候補: 70名
- フラグ付き候補: 34名
- `現在週` はヘッダーのみで、連盟員数0として `戦力分析` が再計算されていた。
- 画像上では参加者数が `91/97` と読める箇所があり、OCRで抽出できた投票名寄せ79件とは差がある。反映前に未抽出分の確認が必要。

## 4. Timeline

- 2026-06-17 22:51-22:52: Dropbox `JDX戦力スクショ/inbox` に `IMG_2340.PNG` - `IMG_2377.PNG` が追加された。
- 2026-06-17 23時台: Apple Vision OCRで38枚を読み取り、投票候補・コメント候補・シート更新候補CSVを作成した。
- 2026-06-17 23時台: `現在週` がヘッダーのみであることを確認した。

## 5. Interpretation

投票者ポップアップは選択肢番号と総戦力を読み取れる。コメント欄スクショは一軍編成・一軍戦力・職業の補完に使えるが、OCRが名前や数値を部分的に誤読する行がある。

今回の `現在週` は空のため、過去の `現在週` との差分として反映することはできない。安全に進めるなら、96名の名簿が残っている `20260531_戦力アンケート` をベースに復元し、今回OCRで確度の高い `C:G` 候補を重ねるのがよい。

ただし、画像上の参加者数とOCR抽出件数に差があるため、投票回答者数の正本はまだ確定していない。前回同様、投票回答者タブを作る場合は、未抽出分を目視または追加OCRで補完してからにする。

## 6. Risks

- `現在週` が空なので、復元なしで反映すると既存の96/97名一覧を失ったままになる。
- OCR抽出の投票名寄せは79件で、画像上の参加者数らしき `91/97` と一致していない。
- コメントOCRには低スコア名寄せや、数値前に余計な文字が混ざる行がある。
- `large_total_delta` は2025-05-31ベースとの比較なので、週次成長として自然なものも含む。

## 7. Recommended actions

1. `現在週` を復元してよいか確認する。復元元は暫定で `20260531_戦力アンケート`。
2. 投票OCRの未抽出分を確認し、今回の投票回答者数を確定する。
3. `safe_to_apply=True` の70名から先に `C:G` 反映し、低スコア候補10名は目視確認後に個別反映する。
4. 反映後に `戦力分析` の連盟員数、投票回答数、一軍帯別合計を再確認する。

## 8. Unknowns

- 今回の正しい投票回答者数。
- `現在週` をどのタブから復元するのが最適か。
- OCRで拾えていない投票者名。
- 低スコアコメント候補10名の正しい一軍情報。

## 9. Files referenced

- `data/2026-06-17_jdx_power_vote_ocr.csv`
- `data/2026-06-17_jdx_power_comment_ocr.csv`
- `data/2026-06-17_jdx_power_sheet_update_plan.csv`
- `data/2026-06-17_jdx_power_ocr_summary.json`
- `tmp/jdx_power_ocr_20260617.txt` (not for commit)
